{
  "name": "wartungstermine-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:web": "pnpm --filter @wartungstermine/web dev",
    "dev:api": "pnpm --filter @wartungstermine/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {}
}
