# Copy this file to .env and adjust if your API is not on the default port.
# Only variables prefixed with VITE_ are exposed to client-side code, and
# they are baked in at build time - see the README for details.

# Base URL of the API (no trailing slash, no /api suffix - the client adds it).
VITE_API_URL=http://localhost:4000
